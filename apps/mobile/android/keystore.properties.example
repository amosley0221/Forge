# Copy to apps/mobile/android/keystore.properties to sign local release builds
# with your own key. The real file is git-ignored — never commit it.
#
# storeFile=/absolute/path/to/forge-release.jks
# storePassword=your-store-password
# keyAlias=forge
# keyPassword=your-key-password
